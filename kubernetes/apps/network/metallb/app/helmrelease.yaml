apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: metallb
spec:
  interval: 15m
  chart:
    spec:
      chart: metallb
      version: "0.16.*"
      sourceRef:
        kind: HelmRepository
        name: metallb
  values: {}
